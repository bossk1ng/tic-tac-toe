'use strict';

const Gameboard = (() => {
    let board = Array(9).fill('');
})();

const Player = (sign) => {
    const sign = () => sign;
    let win = () => false;
};

const display = (() => {})();
